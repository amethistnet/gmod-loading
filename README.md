# gmod-loading

Loading screen for construction site kitchen events.

## Commands

Install dependencies:

```sh
npm install
```

Start the local dev server at `localhost:4321`:

```sh
npm run dev
```

Build the production site to `./dist/`:

```sh
npm run build
```

Regenerate `public/images-manifest.json` from the files in `public/images`:

```sh
npm run images
```

Preview the production build locally:

```sh
npm run preview
```
